    
    let numeros = parseInt(prompt("INGRESA EL LIMITE"));
    let contador = 0;
    let incrementador = 1;
    while (incrementador <= numeros) {
        contador++
        console.log(contador)
        incrementador = incrementador + 1;

    }